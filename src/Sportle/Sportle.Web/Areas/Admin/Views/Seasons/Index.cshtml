@model IEnumerable<Sportle.Web.Models.Formula1.Season>

@{
    ViewData["Title"] = "Seasons";
}

<h1>Seasons</h1>
<p><a asp-action="Create">Add New</a> | <a asp-controller="Home" asp-action="Index">Back to Dashboard</a></p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Year)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <a asp-action="Details" asp-route-id="@item.Id">@Html.DisplayFor(modelItem => item.Year)</a>
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" title="Edit">✏️</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" title="Delete">🗑️</a>
                </td>
            </tr>
        }
    </tbody>
</table>
